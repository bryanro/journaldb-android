<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="@string/pref_name_hours_back"
        android:title="@string/pref_title_hours_back"
        android:defaultValue="10"
        android:entries="@array/pref_hours_back_list_titles"
        android:entryValues="@array/pref_hours_back_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        />
    <EditTextPreference
        android:key="@string/pref_name_host_url"
        android:title="@string/pref_title_host_url"
        android:defaultValue=""
        />
</PreferenceScreen>